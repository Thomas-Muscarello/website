//stores the blog data fetched from the endpoint
import React from 'react';

const BlogContainer = () => {
    return (
        <div>
            this is a single blog
        </div>
    );
}

export default BlogContainer;